/*
//boolean
var VouF = false;
console.log(typeof(VouF));

//number
var number = 1;
console.log(typeof(number));

//string
var string = 'victor';
console.log(typeof(string));

//function
var func = function(){};
console.log(typeof(func));
*/

//declarando variaveis 
/*
//var -> escopo global e local = pode ser chamada em qualquer,pode ter seu valor alterado e se nao tiver valor atribuido de inicio ela sera considerada como null(undefined=indefinido)
var variable ='Feitoza';
variable = 'Victor';
console.log(variable);

//let -> escopo de bloco local = quando a variavel é declarada dentro do bloco de codigo (dentro de uma função por ex.) ,pode ter seu valor alterado assim como VAR e se nao tiver valor atribuido de inicio ela sera considerada como null(undefined=indefinido)
let variable2 ='Victor';
variable2 ='Feitoza';
console.log(variable2);

//const ->possui valor constante e nao é possivel ser reatribuida, possui valor local de bloco e so iniciada quando possuir um valor inicial 
const myName = 'Victor Feitoza';
console.log(myName);
*/

//testing scopes
/*
var global = "global";
console.log(global);

function local() {
    let localScope = "intern";
    console.log(localScope);    
    // nã seria possivel imprimir o valor da variavel se o metodo tivesse escrito fora da função local
}
*/
/*
//atribuindo valores á variaveis
o sinal de = significa atribuição, por ex var nome = "victor"

//comparando valores
a comparação é feita usando == e sempre retorna um valor boolean, por ex "0"==0 que retornará true pois ambos valores são 0 independente de serem de tipos diferentes

//comparação identica 
compara o valor e o tipo do que está sendo comparado, por ex '0'===0 que retornaria falso porque um dos valores é string enquanto o outro é int
*/